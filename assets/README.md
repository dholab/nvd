# Samplesheet Assets

The samplesheet CSVs in this directory are examples for authoring and documentation.
Rows that contain placeholder local FASTQ paths, such as `/data/run42/...`, are
illustrative only and will not pass filesystem-sensitive validation until those
paths are replaced with files or glob matches visible in your run environment.
